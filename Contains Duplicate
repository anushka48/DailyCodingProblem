class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """

        hashMap = dict()
        for num in nums:
            if (hashMap.get(num)): 
                return True
            else:
                hashMap[num] = 1

